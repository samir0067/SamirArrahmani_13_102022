import React, { FC } from "react";
import Hero from "components/Hero";
import Features from "components/Features";

const Home: FC = () => {
  return (
    <div>
      <Hero />
      <Features />
    </div>
  );
};

export default Home;
